using System;
class HelloWorld {
    
  static void addRandomElToArray(int[] array, int ind)
  {
    Random rnd = new Random();
    array[ind] = rnd.Next(100);
  }
    
  static void Main() {
    readonly string[] surnames = new string[7] {"Armstrong", "Hawkins", "Raev", "Carter", "Decker", "Cerruli", "Biesler"};
    
    // a)
    int[] rezults = new int[7];
    for (int i = 0; i < rezults.Length; i++)
    {
        addRandomElToArray(rezults, i);
        Console.WriteLine(rezults[i]);
    }
    
    
  }
}
